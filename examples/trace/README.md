# Worked Trace (placeholder)

This directory will hold **one complete engagement slice, end to end**, from the pilot engagement (sanitized to synthetic names per the customer-IP rule):

1. `/ai-sdlc:init-engagement` output — the scaffold as committed
2. Corrected `context/` files — showing what the human changed from the AI drafts (the interesting part)
3. One requirement → `/ai-sdlc:spec` output → the human-approved spec
4. The `/ai-sdlc:implement` result and `/ai-sdlc:review` findings
5. The PR as merged

Populate after the pilot engagement completes its first full slice. Until then, [QUICKSTART.md](../../QUICKSTART.md) is the reference for the flow.
