# Conventions

> **Human-owned, machine-read.** How THIS codebase does things. Specific beats general — "controllers in `/api`, services in `/services`, no logic in controllers" is useful; "write clean code" is not.

## Structure

[Where things live; layering rules]

## Naming & Style

[Naming conventions; formatting beyond what the linter enforces]

## Patterns

[Error handling, logging, DI, data access — the patterns to imitate, with a pointer to one exemplar file each]

## Anti-Patterns

[Things that exist in the codebase but must NOT be imitated]
